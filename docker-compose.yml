version: '3.8'

services:
  product-feed-comparator:
    build: .
    container_name: product-feed-comparator
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
